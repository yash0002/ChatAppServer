const express = require('express');
const models = require('../models/login_model');

exports.login_service_function = function(err, data) {
    if(err) {}
    else {
        console.log('data');
        models.login_model.js;
    }
}

